//Create event
image_xscale = 0.1;
image_yscale = 0.1;

//Step event 
image_xscale = image_xscale + 0.05;
image_yscale = image_xscale;
image_alpha = image_alpha - 0.01;


if(image_alpha <= 0)
{
	instance_destroy(self);
}

//Collision with enemy parent 
with(other)
{
	obj_enemy_parent.hp -=1;
}
//Collision with Player object or Parent 
with(other)
{
	obj_player.hp -=1;
}
